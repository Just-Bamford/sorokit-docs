# Configuration

Document shared configuration options here as the package APIs stabilize.

## Suggested Shape

```ts
type SorokitConfig = {
  // Add stable options here as the SDK evolves.
}
```

## Guidance

Configuration should be:

- Explicit
- Easy to validate
- Friendly to TypeScript inference
- Portable across environments
